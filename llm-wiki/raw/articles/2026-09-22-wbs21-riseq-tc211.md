# 2026-09-22 — WBS 2.1 first step: riseq master clock + TC-2.1.1 10-minute pin (TDD, audit 0/0)

> Source: session evidence (test output, audit, mutant run), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. TDD work unit (RED watched, mutant-caught, GREEN verified, audit
0/0). First M2.2/2.1 code: nothing named `riseq` existed before.

## What (minimal, TC-2.1.1 only)
- `engine/seq/riseq.h` / `riseq.c`: sample counter + owned tempo map;
  `RiSeqInit` / `RiSeqAdvanceFrames` / `RiSeqMasterClock`. WBS Adaptation
  (documented in-header): Create→Init because Phase-0a bans heap
  allocation in engine/ (caller owns the struct); Advance added
  (render must advance; no other mutator); tick queries deferred to
  snapshot/walker work. Rate locked RI_AUDIO_SR; ao carried opaquely
  (NULL on host/CI); bring-up map single-segment 120 BPM (tempo loading
  arrives with TC-2.1.3).
- `tests/unit/t21_seq.c`: 10 sim-minutes of 64-frame buffers at 48 kHz
  (450k boundaries) + 44.1 kHz map sweep (413k, rate-independence):
  counter exact every buffer, map agreement within +-1 at
  integer-ideal boundaries only (nearest-tick quantization elsewhere is
  expected — an early +-1-everywhere bound failed correctly and was
  corrected by math, not by loosening), final totals exact (115200
  ticks both rates), coverage guard (exact_points > 0).
- `scripts/ri_build_host.sh`: `MOD_sched` gains `riseq.c`; new audit
  Phase 6b (files + sched build + t21_seq).

## Proof
- RED watched: missing `riseq.h` (feature-missing, not typo).
- Load-bearing proven by mutant (`samples += frames + 1` → `FAIL ...
  clock 65 want 64`, exit 1) with revert verified.
- GREEN: `PASS t21_seq`; full `ri_audit.sh` 0/0 (incl. new Phase 6b).
- Gotchas: audit Phase-0a greps banned tokens literally — a comment
  NAMING the ban trips it (reworded); `test` binaries must exit
  nonzero on failure (already fixed this session).

## Files (uncommitted)
- `engine/seq/riseq.h`, `engine/seq/riseq.c`,
  `tests/unit/t21_seq.c`, `scripts/ri_build_host.sh`,
  `scripts/ri_audit.sh`.