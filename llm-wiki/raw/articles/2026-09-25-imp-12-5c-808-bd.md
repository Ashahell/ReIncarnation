# §12.5c — BD 808-regime remodel (paper-read, E1 numbers, honest scope)

**Date:** 2026-09-25 (host lane; devices untouched)
**Scope:** review §4.2 item 4 (BD) + SD structural verification + recursive
envelopes (held). Closes §12.5.
**Disposition:** New (TDD RED-first; regime shift with paper grounding)

## Research (paper-fetched, not memory)

E1 Werner/Abel/Smith DAFx-14 (PDF fetched 2026-09-25): BD center ~56 Hz
(Roland chart), bridged-T as band-amplifier pinged by pulse-shaped inputs,
Q43 start oscillation, pitch sigh from R161 leakage (fitted memoryless
nonlinearity in the paper), output stage = passive LP (Tone!) + level + HP.
Full WDF/MNA topology is a fitted system (SPICE-fit nonlinearity, Gen impl)
— disproportionate without measurement validation. Implemented: the regime
(50–60 Hz boom, fast sigh, damping decay, click); topology noted as the
next step with the paper reference in the BD ledger.

## Production changes

- `RI_808_BD_F_START` 170→62 Hz, `RI_808_BD_TAU_PITCH` 22→4 ms sigh
  (F_END 48, tune ±7, damping tau, click all untouched).
- SD verified structural (two partials + Tune + Snappy + HP/LP noise —
  everything §4.2 item 5 describes is present and wired); P-08 re-measure
  stays open for captures. No SD code touched.
- Recursive envelopes HELD (§12.1-consistent: rest bounds t, total exp
  safe, cost to the bench pass).
- t1_808 §1 + t23_808bd trajectories deliberately re-based (same ±5% method).

## TDD record

`t43_808_bdboom` (7-FAIL RED → PASS): pitch law exact ±1%, early regime
(ZCR past the click window — click contaminates raw ZCR), settle rate,
click presence, tune base. t42's BD-tone meter moved off broadband peak
(phase-fragile post-remodel) to the 2.5 kHz click band; an unmeasured
"5.6x" comment claim caught and removed.

## Deliberate re-baselines (each root-caused)

- bd + storm only (pitch-law change; amp path untouched — tails match to
  6 decimals; diffs from sample 0). All other 808 goldens identical.
- BD ledger re-based (P-07 → regime; paper findings + Tone-LP future noted).

## Gates

- 13/13 808-family tests green. Full `ri_audit.sh` 0/0 — all 14 phases.
