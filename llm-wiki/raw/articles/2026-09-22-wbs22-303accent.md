# 2026-09-22 — WBS TC-2.2.2 accent envelope (TDD property pin)

> Source: session evidence (test output, mutant run, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. TDD property pin (PASS watched on frozen code, scratch-mutant
caught, GREEN verified, audit 0/0).

## What
- `tests/unit/t22_303accent.c`: accent trigger → peak exactly 1.0;
  sample-stepped render asserting monotone nonincreasing decay (no
  reset bumps) and 1/e crossing inside 2592..3168 samples (±10% of
  60 ms @48 kHz). White-box on struct fields.
- Wired with the 303 audit phase. No production code changed.

## Proof
- PASS first run (expected: property pin over landed P-02 code).
- Load-bearing proven by scratch-copy mutant (tau 60→120 ms in a
  scratch copy — frozen file untouched): no 1/e crossing inside the
  4800-sample loop, exit 1. Mutant discarded after. (A ±15% tau would
  fail inside the window proper; the 2x mutant fails at the loop
  bound — both are catches.)
- Full `ri_audit.sh` 0/0.

## Files (uncommitted)
- `tests/unit/t22_303accent.c`, `scripts/ri_audit.sh` (303 phase).