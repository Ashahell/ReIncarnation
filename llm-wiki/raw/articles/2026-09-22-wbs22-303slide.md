# 2026-09-22 — WBS TC-2.2.3 slide legato (TDD property pin)

> Source: session evidence (test output, mutant run, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. TDD property pin (RED behavior watched, scratch-mutant caught,
GREEN verified, audit 0/0).

## What
- `tests/unit/t22_303slide.c`: two-note slide retrigger on a held
  voice — env does not reset (field equality at the slide call +
  monotone decay after, no jump), gate stays high, pitch slews
  (still >10% off after 1 sample) and reaches within 1% inside
  5·τ_slide (9600 samples @ τ=0.040). Both entry forms: `rb303_note`
  slide retrigger + `rb303_slide_to` target-only check. Sample-stepped
  render for trajectory asserts; wired with the 303 audit phase.
- No production code changed (voice frozen green).

## Proof
- PASS first run (expected for a property pin over landed code).
- Load-bearing proven by scratch-copy mutant (slew replaced with
  instant assignment in a /tmp copy — frozen file untouched):
  `FAIL jumped instantly 164.81 vs 164.81`, exit 1. Mutant discarded
  after (not committed, not kept).
- Full `ri_audit.sh` 0/0.

## Files (uncommitted)
- `tests/unit/t22_303slide.c`, `scripts/ri_audit.sh` (303 phase).

## Standing 2026-09-22 update: tau_slide 40 ms provisional-good
- Operator ear check on glide feel: hard to judge by ear alone.
- Decision: keep 40 ms (P-03 candidate default stands, marked good for
  now). The M2.2 blind 40-vs-60 A/B (spec OPEN-01) stays OPEN — this is
  a provisional accept, not the verdict.