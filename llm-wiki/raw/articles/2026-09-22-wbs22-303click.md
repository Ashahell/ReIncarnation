# 2026-09-22 — WBS TC-2.2.5 waveform-click flag (feature, TDD)

> Source: session evidence (test output, mutant run, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. First production DSP change of the M2 work (all prior turns were
pins/probes): `classic_click` flag + 0.5 ms crossfade, TDD (RED watched,
mutant-caught, GREEN verified, audit 0/0).

## What
- `engine/dsp/rb303.h`: `classic_click` (default 1 = current behavior),
  `wave_rendered`, `xfade_n` fields.
- `engine/dsp/rb303.c`: per-sample shape select becomes a branch —
  classic hard-switches (byte-identical to the old direct read: goldens
  re-verified), flag-off runs a 24-sample (0.5 ms @48 kHz, sr-scaled)
  crossfade between old/new shapes; double-toggle restarts the ramp
  (documented, TC-silent).
- `tests/unit/t22_303click.c`: both voices to phase ~0.75, flip wave,
  render 64 — smooth max step < 0.1, classic > smooth (ordering,
  phase-independent), classic > 0.05 absolute (characterization:
  measured 0.0965 post-ladder vs 0.0193 smooth).
- Spec source: Deep Dives Part 1 §1 ("0.5 ms crossfade plus a tiny
  deterministic click on toggle — flag it in classic mode").

## Proof
- RED watched: no `classic_click` member (feature-missing).
- Load-bearing proven by mutant (crossfade condition forced false →
  smooth == classic → ordering fails exactly) with revert verified.
- Full `ri_audit.sh` 0/0 — incl. 303 golden sha256 (classic path
  byte-identical) + one transient quota-race FAIL on the AROS phase
  (standalone green, retry clean).

## Files (uncommitted)
- `engine/dsp/rb303.h`, `engine/dsp/rb303.c`,
  `tests/unit/t22_303click.c`, `scripts/ri_audit.sh` (303 phase).