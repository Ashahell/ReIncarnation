# 2026-09-22 — WBS 2.1 staged swap arbiter + 10k continuity soak (TC-2.1.3 event half, TDD)

> Source: session evidence (test output, mutant runs, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. TDD work unit (RED watched, mutants caught, GREEN verified, audit
0/0). The event half of TC-2.1.3 (boundary application + continuity);
the PCM click half waits on voices.

## What (minimal)
- `RiSeqRequestSnapshot` (stage, GUI side) + `RiSeqBeginBuffer`
  (apply staged→active at next buffer start, render side) on the seq
  object (+2 pointer fields, no alloc). Contract: request only between
  buffers; NULL request cancels; last request wins.
- `tests/unit/t21_seqswap.c`: edge pins (fresh-NULL, cancel, double,
  plus the load-bearing preset-cancel) + 10k-swap soak (LCG offsets,
  6 buffers/round): per buffer the active snapshot is asserted, and
  every in-window event of the active song is asserted delivered
  exactly once (no dup, no loss; old-song tails past the boundary
  abandoned by definition).

## Proof that the test discriminates staging (earned the hard way)
- First "staging proof" attempt (deliver via a held local pointer)
  passed under an immediate-apply mutant — vacuous, because observing
  through BeginBuffer applies. Removed, not kept.
- The preset-cancel edge (`Load A, Request B, Request NULL, Begin must
  return A`) genuinely discriminates: guarded immediate-apply mutant
  fails exactly there (`null-cancel keeps active`, exit 1); correct
  code passes. Staging vs immediate is otherwise unobservable
  single-threaded (same return values on all other sequences) — the
  residual SMP tear property is documented as render-loop contract,
  proven later under threads/RT analysis.
- Process catch: one FAIL run was a STALE build (mutant .o still in
  OUT/ — always rebuild module objects before running the test).
- Full `ri_audit.sh` 0/0.

## Files (uncommitted)
- `engine/seq/riseq.h`, `engine/seq/riseq.c`,
  `tests/unit/t21_seqswap.c`.