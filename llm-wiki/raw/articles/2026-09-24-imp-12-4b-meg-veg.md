# §12.4b — Dual envelopes: MEG/VEG split + accent sweep (spec direction, E1 values)

**Date:** 2026-09-24 (host lane; devices untouched)
**Scope:** review §4.1 envelope architecture (the biggest sonic gap); second
vertical third of §12.4. Log-domain slide + osc/filter held for measurement.
**Disposition:** New (TDD RED-first; deliberate revoicing with lineage values)

## Design (spec-authorized, E1 Open303-lineage starting values)

- MEG (filter): Decay-knob τ, forced 200 ms minimum on accented notes.
- VEG (amp): fixed 1.23 s normal / 200 ms accented.
- Accent sweep: `sweep += amt·(1−sweep)` per accent (saturating "wow"),
  discharged per-sample with reso-lag τ = 50 ms + 50 ms·reso_k (E0);
  drives cutoff at ×0.5 (single-accent peak ≈ old term).
- Releases 0.5 ms normal / 50 ms accented (replaces the 5 ms hypothesis).
- Gate closes the VCA via release; MEG keeps its note decay; slide
  retriggers neither (t22 now pins both envelopes).
- Recorded tension: Devil-Fish VEG fixed 3–4 s vs lineage 1.23 s — open
  for the §8 measurement pass, not decided here.

## TDD record

`t39_meg_veg.c` (compile-RED → PASS in 0.17 s): (a) decay knob moves MEG
(0.08→~0, 4.0→0.78) while VEG matches to 0.02; (b) accent forces min MEG
(0.082 < 0.15 vs plain 0.88 > 0.8 at 0.5 s); (c) sweep builds across accents;
(d) releases (plain ~0, accent 0.64 > 0.5 at +10 ms); (e) audibility guard.
Two test-side fixes: volume-law threshold corrected to the taper (carried
from t36's lesson — check the table first); infinite loop in the render
helper from float residue (integer countdown now).
`t22_303slide` deliberately updated to pin both envelopes (was single-env).

## Deliberate re-baselines (each root-caused)

- first-light.{wav,events→identical,441,aiff} + sha: first PCM diff at
  sample 4 (1 LSB — the taus separate from the first decay step); same
  length; tails exact 0; peaks healthy. (Process: a shell-variable bug
  faked an EVENTS-DIFFER — md5 proved identical; a quick AIFF parse
  mis-sliced SSND — audit cmp is the authority.)
- sched-check.wav + sha: events 12/12 identical (fixture ties everything —
  elegant proof the gate rule is a no-op there); WAV from sample 4, pure
  revoicing. `compare` IDENTICAL on the new pair.
- 303 accent-behavior pins (t22_303accent) green unmodified — the revoicing
  preserves the documented boost.

## Gates

- Full `ri_audit.sh` 0/0 — all 14 phases.
- Owner A/B flagged: first-light now sounds different (sustain reshaped);
  lineage values are starting points per spec, M2.2/measurement decides.
