# §12.5b — Metal fixed freqs + choke + per-sound params (E1 + manual p. 35)

**Date:** 2026-09-25 (host lane; devices untouched)
**Scope:** review §4.2 items 3/7/8 (metal, accent, choke) + per-sound
Tune/Decay/Snappy/Tone wiring. BD/SD remodels stay open (§12.5c).
**Disposition:** New (TDD RED-first with per-family revert-checks)

## Production changes

- Six fixed oscillators (205.3/304.4/369.6/522.7/540/800 Hz, E1
  Werner/Abel/Smith) shared by CY/OH/CH; per-voice HP kept (7 kHz hats,
  5 kHz CY); squares PolyBLEP'd (naive helper removed); CB pair kept.
- Choke: CH trigger kills ringing OH; OH within 50 ms after CH pre-rolls
  to ~1e-3 (very short OH); late OH rings normally. Knob state untouched.
- Tune honored by toms/congas/SD-body (`tune_ratio`, ±7 clamp shared);
  Snappy = SD noise ratio (knob/64); Tone = BD click gain + CY HP cutoff
  (5000·2^((v−64)/48)). All exact at defaults (golden-proof below).
- `t23_808hat` re-contracted to the E1 set incl. CY; `t1_808` §2 follows.

## TDD record

`t42_808_metal_choke` (18-partial RED + choke-leak RED → PASS): fixed
partials (Goertzel transfer), choke both directions + negative control,
snappy @1800 Hz (12.8x measured, bound 5x — RMS was a 2% meter),
BD click peak (1.31x, bound 1.2x), CY cutoff ratio @304 Hz (13.6x,
bound 4x — the naive RMS expectation was inverted; the ratio direction
IS the filter law), tom/SD tune ratios (exact 1.49831).
CH 205.3 Hz documented below method floor (HP stopband + 35 ms decay);
pinned via OH/CY (shared path).

## Deliberate re-baselines (each root-caused)

- ch/oh/cy/cb/storm only (metal + blep); other 11 byte-identical —
  exact-default preservation proven in production.
- Anatomy: same lengths/peaks (PolyBLEP preserves levels), tails zero,
  diffs from first samples. Ledger notes on all four voices.

## Gates

- 12/12 808-family tests green. Full `ri_audit.sh` 0/0 — all 14 phases.
