# 2026-09-22 — WBS TC-2.1.4 DSP-side storm budget (TDD, host proxy)

> Source: session evidence (test output, mutant runs, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. TDD work unit (RED watched, mutants caught, GREEN verified, audit
0/0). DSP-side storm (all 4 sections through their own renderers);
core-path storm waits on device wiring.

## What
- `tests/unit/t21_storm.c`: 303A (45/accent) + 303B (50/plain) with
  auf_voice_defaults params, all 15 808 (max decay, accented), all 6 909
  (baked sine layers, accented) into one 64-frame buffer @48 kHz, timed
  with `clock()` (t1_808 method + INFO line). Asserts: ratio ≤ 0.5,
  mix peak > 0.01, per-section peaks > 0.01 each, re-render
  bit-identical.
- Measured: ratio **0.459–0.463** (308 µs vs 667 µs budget) — passes
  with THIN margin (~2.2x headroom on this host). The AROS-box run
  remains open (this is a host proxy); a KVM-guest or Dell run would
  be the genuine article.
- Findings en route: per-section asserts caught real silence (303B
  with slide-from-silence renders nothing — gate needs a held note;
  coherent voice semantics, storm uses plain notes); the mix-then-split
  refactor used per-section buffers after a bad sed melted four
  accumulate loops into one (editor tool after).

## Proof
- RED: missing test target (fresh file, nothing references it).
- Load-bearing: budget-tightening mutant fires; silent-section would
  fire per-section asserts (proven by the real 303B silence above).
- Full `ri_audit.sh` 0/0 (Phase 6b runs 10 seq tests now).

## Infra note (session environment, shared)
- `/tmp` usrquota blocked compiles/links/audit 3+ times today (live
  concurrent session bursting). Fix applied: `/tmp/ri/build` and
  `/tmp/ri/run` moved to `/home/miller/Work/ri_tmp/` with symlinks
  back (all conventional paths keep working; 10 MB probe writes fine).
  Durable fix (RI_TMPDIR override in scripts) is follow-up, not this
  turn. Only own scratch deleted; other sessions' data untouched.

## Files (uncommitted)
- `tests/unit/t21_storm.c`, `scripts/ri_audit.sh` (Phase 6b).

## Standing 2026-09-22 update: held-note slide covered (was open above)
- 303B now plays note 50 + `rb303_slide_to(52)` mid-buffer (held note →
  slew): slide storm renders sound AND differs from plain (asserted
  both; RED watched as "slide inaudible (identical to plain)" before
  the call was wired; ratio 0.474, still green).
- Slide-from-silence stays silent by voice semantics (gate needs a held
  note) — coherent, not a bug. Both slide behaviors now pinned.

## Standing 2026-09-22 update: Dell run GREEN (TC-2.1.4 on reference hardware)
- Cross-built for ABIv11 (v11 gcc + SDK, `-ldos -lexec`, default spec
  supplies `-lm`; DSP needs NO libm — ri_* kernels only; `uname`/`clock`/
  `printf` all work on AROS) → `storm_dell` (55 KB, task.resource=0).
- Dell (E6320, session 750): `machine=AROS/x86_64 ratio=0.9000` → PASS
  (0.45× buffer ≤ 0.5×). THIN margin (10% headroom) — watch item, not a
  failure; a cold CPU or background load could flake it.
- Path correction (honest): the first Dell FAIL (0.93) was a TEST bug,
  not hardware — the assert double-halved the budget (`ratio <= 0.5`
  against an already-halved budget = 0.25× buffer). Fixed to `<= 1.0`
  (t1_808 shape). The `-march=sandybridge` experiment correctly ruled
  out codegen (0.93→0.90, no lever there) before the real bug surfaced.
- Open curiosity (not gate-relevant): host t21 binary measures ~304 µs
  vs ~141 µs for an equivalent scratch harness, rock-stable both ways
  (governor ruled out). Unexplained; parked.

## Standing 2026-09-22 update: repeat loop + assert shape (were unrecorded)
- `T21S_REPS 1000u`: the budget loop repeats the storm 1000× and divides
  (single 64-frame storms are sub-tick on coarse clocks — the Dell read
  0.000 s before this; now `cpu=0.0006s` is a real number). Functional
  asserts (energy, determinism, slide) use single renders outside the
  timing loop.
- Assert is `ratio <= 1.0` where ratio = cpu/(0.5×buffer) (t1_808
  shape). An earlier `<= 0.5` double-halved the budget (demanded
  0.25×); the first Dell FAIL was that bug, not hardware.
- 303B slide coverage lives in the same test via a flag:
  `storm(out, slide)` renders the second half with `slide_to(52)` when
  set; main runs plain (timing + energy + determinism) and slide
  (sound + differs-from-plain asserts) variants.