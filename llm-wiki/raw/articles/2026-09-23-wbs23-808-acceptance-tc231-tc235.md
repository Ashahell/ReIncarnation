# 2026-09-23 — WBS 2.3 TR-808 voice acceptance: TC-2.3.1–2.3.5 (TDD pins + metal-ratio lock)

> Source: session evidence (test output ×6, audit 0/0, golden regen), compiled by agent
> Collected: 2026-09-23
> Published: 2026-09-23

## Disposition
New. The engine already carried the fifteen 808 voices (Task 8 / gate G8,
landed `29542b7b`); this slice adds the WBS 2.3 per-TC acceptance pins
(`t23_808hat/bd/clap/accent/storm`, wired in `ri_audit.sh` Phase 8 after
`t1_808`) — and the evidence they demanded moved one real production
constant: the hat metal cluster base.

## What
- **Metal-ratio lock (production change, the one the pins demanded).**
  `engine/dsp/rb808.c` ratio array → the WBS TC-2.3.1 contract
  {0.83, 1.48, 2.26, 2.92, 3.94, 5.31}; `RI_808_METAL_BASE` 400 → 1000
  (`rb808.h:58`, comment cites TC-2.3.1 + WBS ratios + deep-dive pt 3) →
  partials 830/1480/2260/2920/3940/5310 Hz. CY keeps the LOW base 250 with
  the shared ratio set (208/370/565/730/985/1328 Hz). `t1_808.c:110` literal
  `400.0` → `(double)RI_808_METAL_BASE` so the parent mirror tracks the
  constant.
- **808 param section** (WBS interface line 81): `engine/dsp/params.c`
  gains the 808 block — `RI_808_DECAY_TBL` (9 anchors
  {0.18,0.22,0.28,0.38,0.50,0.72,1.10,1.80,2.80}: knob 0 → 0.18 s,
  64 → 0.50 s, 127 → 2.8 s) and `rb808_set_param(struct RB808Voice*,
  uint32_t ctl_id, uint8_t value)` after `rb303_set_param`; decl in
  `rb808.h`; ctl ids `RI_CTL_808_BASE+0..5` = LEVEL/TUNE/DECAY/SNAPPY/TONE/
  ACCENT (panels.c order; defaults 100/64/64/64/64/64). DECAY → `tau_amp`
  (interp9); TUNE → `tune_st = (value−64)×14/127` (±7 st); LEVEL/SNAPPY/
  TONE/ACCENT → documented placeholder breaks (no engine field; ACCENT
  trigger-time binary). No new `RB808Voice` fields.
- **Five per-TC pins:**
  - `t23_808hat` (TC-2.3.1 metal half): Goertzel at fc vs fc×0.995/1.005,
    assert local prominence `m0 > mlo && m0 > mhi` only (no absolute floor —
    7 kHz HP attenuates 830 Hz), hardcoded fc {830,1480,2260,2920,3940,5310},
    pins CH + OH.
  - `t23_808bd` (TC-2.3.1 pitch + TC-2.3.2 decay): trajectory ±5% mirror of
    t1 §1 + white-box `tau_amp` 0.18/2.8 ±10% at knob 0/127 + rendered 1/e
    crossing (64 ms windowed RMS — the 5 ms window was shorter than the
    48 Hz settled period, phase-dependent reads; window ratio cancels so
    crossings match `(t + w/2)/48000`).
  - `t23_808clap` (TC-2.3.3): 4 peaks + first-3 gaps 9 ms ±2 ms
    (7–11 ms window).
  - `t23_808accent` (TC-2.3.4): all 15 voices +3.522 dB ±0.5.
  - `t23_808storm` (TC-2.3.5): mask 0x7FFF, ≤0.3× via `clock()`,
    bit-identical re-render. "14-voice" in the TC, engine has 15 — superset
    covers, noted honestly.
- **Golden regen**: the metal lock alters exactly ch/oh/cy/storm (the metal
  cluster voices); the other 11 voices byte-identical (re-render-compare
  before install). Goldens reinstalled + `.sha256` sidecars rewritten
  (two-space `hash  tests/golden/808/NAME.wav` format, `sha256sum -c` clean).
- `scripts/ri_audit.sh` Phase 8: five `test t23_*` lines after the
  `t1_808` line, each with its TC tag in the FAIL echo.

## Proof
- TDD RED first, all watched: `t23_808hat` exit 1 against the pre-lock
  engine (8/12 FAIL — the 4 passing partials were coincidental harmonics of
  the 400-base cluster; the prominence rule is the right detector); `bd`
  exit 1 as a build-RED (`rb808_set_param` / `RI_CTL_808_DECAY` undeclared —
  feature-absent manifest, C99 implicit-decl); clap/accent/storm were green
  pins on frozen code (property pins over landed behaviour: clap peaks
  0.21/9.5/18.2/27.3 ms, gaps 9.3/8.7/9.1 ms).
- GREEN after the lock + param section: `t23_808hat` m0 runs 5–45× above
  its ±0.5% skirts at all 6 contract freqs, both CH and OH; `t23_808bd`
  white-box + rendered crossings inside ±10%; all five exit 0, 0 FAILs.
- Full `ri_audit.sh` 0/0 (audit phases incl. Phase 8 re-render determinism
  over all 16 goldens via `$OUT/render`; the stale-render trap is on
  record below).
- Golden regression: 12 non-cluster voices re-rendered byte-identical to
  their committed sidecars; the 4 cluster voices moved and were installed
  with matching sidecars.
- Stale-tool gotcha worth keeping: `ri_build_host.sh all` compiles MOD
  objects only — `$OUT/render` is linked by ri_audit.sh (line 36) against
  `$OUT/*.o`. Tests link fresh `.o` at dispatch (so `t23_808hat` passed the
  contract freqs), but a hand-run golden render used the 14:54-prebuild
  binary and was byte-identical to the committed goldens — the metal change
  did NOT show. Relinking `render` against the fresh objects produced the
  expected 4/12 split. Lesson: golden re-renders must run the relinked
  tool, not whatever `$OUT/render` says.

## Files
- env: `engine/dsp/rb808.c` (ratio array → contract), `engine/dsp/rb808.h`
  (`RI_808_METAL_BASE` 1000, ctl-id block, `rb808_set_param` decl),
  `engine/dsp/params.c` (808 section), `tests/unit/t1_808.c` (§2 base),
  `scripts/ri_audit.sh` (Phase 8 t23 lines)
- tests: `tests/unit/t23_808hat.c` `t23_808bd.c` `t23_808clap.c`
  `t23_808accent.c` `t23_808storm.c` (new)
- goldens: `tests/golden/808/{ch,oh,cy,storm}.wav` + `.sha256` (regenerated)
- ledgers: `docs/evidence/808/{ch,oh,cy,bd}.md` → LOCKED
- wiki: this article + `llm-wiki/log.md` + `llm-wiki/index.md`

spirv-val vacuous (no SPIR-V in this repo — noted honestly per standing).