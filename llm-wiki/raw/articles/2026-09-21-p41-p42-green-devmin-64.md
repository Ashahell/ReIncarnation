# 2026-09-21 — P4.1/P4.2 green on VOID: dev_min_frames=64, audit 0 errors

> Source: session evidence (guest serial PDBG markers, probe result blocks, host audit log), compiled by agent
> Collected: 2026-09-21
> Published: 2026-09-21

Follow-up to the VOID Stop/HookEntry record. Consultant ladder executed to
completion on the codec-less guest (all shadows as before: newsafe
ahi.device, patched sb128, instrumented hdaudio, instrumented VOID).
Baseline carried in: prior TRUE-WaitIO/CloseDevice green run, rc=0 in
7714 ms with `CloseDevice done` and M46/M43 present.

## P4.1: two independent NULL-link requests, TRUE WaitIO, no aborts

- `P4.1 WaitIO r1 done err=0`, `P4.1 WaitIO r2 done err=0`.
- Both complete naturally. Device + VOID path fully functional; nothing
  wrong with second requests per se. Link semantics isolated as the only
  remaining variable (now closed below).

## P4.2: linked double-buffer ladder fully green

- `CheckIO r1=REPLIED r2=REPLIED` on all 7 rungs (4096–64 frames).
- `err1=0 err2=0` every rung; `dev_min_frames=64`.
- rc=0, SUMMARY complete (`low_min_frames=0 dev_min_frames=64
  verify_obs=0 verify_exp=0 dev_open_rc=0`), agent alive, zero IRQ lines.
- P1–P3 skip: no `ahi.library` on the CD (environmental, not a bug).

## Linked-r2 question closed

The earlier r2-pending observation was cold-start timing: once primed
(P4.0/P4.1), linked double-buffering completes every pass. Circular
`ahir_Link` r1↔r2 is not itself the problem; the earlier fault was purely
the unopened r2 (NULL device/unit, fixed by the documented copy method in
`audio_io/probe_ahi.c`).

## Vulkan4AROS audit

`scripts/aros_audit.sh` (3735 lines), run in background during the guest
cycle: **0 errors, 2 warnings — "ready to build."** The audio work touches
only `/tmp` shadows, the local `/tmp/ari-hda-fix` worktree, and the
ReIncarnation repo; nothing in the audited tree regressed.

## Standing state (supersedes prior "open" notes on playback)

- M1.1 device ladder on codec-less VOID: fully green with a measured
  minimum (`dev_min_frames=64`).
- Still open, non-blocking: ZZTEST lddemon-cache experiment; durable
  sb128-driver rebuild via make flow; formal HUNK-alignment build rule;
  uncommitted diffs (repo `probe_ahi.c` r2-copy; worktree markers + SB128
  volatile fix).
