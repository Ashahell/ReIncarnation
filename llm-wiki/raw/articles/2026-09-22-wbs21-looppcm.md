# 2026-09-22 — WBS TC-2.1.2 PCM half green: loop double-render equality (TDD)

> Source: session evidence (test output, mutant run, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. Closes TC-2.1.2 (math half was the loop cursor; this is the PCM
half): doubled 8-step loop renders iteration-identical.

## What
- `tests/unit/t21_looppcm.c`: 16-step song at tempo 120 (pluck at steps
  0 and 8, rests elsewhere) rendered once via the file path; PCM bytes
  [44,44+96000) vs [44+96000,44+192000) (8 steps × 6000 samples × 2 B)
  must be byte-identical. File size guard (204044 = last NOTE_OFF @54000
  + 1 s tail). Mismatch reports max abs int16 diff + first diff sample.
- Settle-gap design (not reset design): 7 silent steps let the pluck
  tail settle, so iteration 2 starts from init-identical state. A
  sustain-across-boundary loop would need loop-reset semantics — open,
  explicitly out of scope here.
- Audit Phase 6b runs it (6 seq tests now).

## Proof
- Initial size-guard failure was test arithmetic (12000 vs 6000
  samples/step), corrected from the writer source (16-bit mono); the
  render itself was correct throughout.
- `PASS t21_looppcm` — halves byte-identical.
- Non-vacuity proven by mutant (second pluck 45→47 → max diff 30752
  from sample 1, exit 1) with revert verified: offsets are correct,
  regions genuinely compared.
- Full `ri_audit.sh` 0/0.
- Process note: one inline-heredoc edit used for the mutant (against
  the repo editing rule — worked, but the rule exists because it
  silently breaks; reverted via editor tool).

## Files (uncommitted)
- `tests/unit/t21_looppcm.c`, `scripts/ri_audit.sh` (Phase 6b).