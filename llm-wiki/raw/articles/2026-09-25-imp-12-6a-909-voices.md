# §12.6a — 909: five instruments + per-voice Level/Decay (11 voices)

**Date:** 2026-09-25 (host lane; devices untouched)
**Scope:** review §4.3 items 1 + 4 (instruments + per-voice Decay/Level).
Accent/flam decouple, shared CH/OH level, 909 clip stay open (§12.6b).
**Disposition:** New (TDD RED-first with revert-checks)

## Production changes

- IDs LT/MT/HT/RS/CP (6–10), `RI_909_NVOICES` 11; per-voice `level`
  (knob/127, default 1.0 exact) + `decay_tau` (0.05–8 s exp feel,
  bypass ≤ 0); LEVEL/DECAY knobs wired (TUNE pre-existing; FLAMRES stays
  owner-approved placeholder).
- Bake: swept-VCO + click extension (incremental phase, hash click);
  legacy branches byte-identical. BD revoiced (150→50 Hz pitch env +
  click); LT/MT/HT swept toms; RS blip+click; CP noise burst. New
  recipes are E0 starting points (P-08 re-measure open for captures).
- Render maps/durations/`--909 wants` extended; t1_909 bad-voice → 11.

## TDD record

`t44_909_newvoices` (BADARG/silence RED → PASS): per-ID smoke (audible,
finite, idles), level law exact ratio, decay audibility halving,
bypass transparency (twin renders bit-identical). Revert-checks on both
knob laws (exact-ratio FAILs → restore → PASS).

## Deliberate golden action (each root-caused)

- bd revoiced (sweep + click); 5 new goldens (peaks/RMS/tails healthy,
  audibility floors clear with margin).
- sd/ch/oh/cr/rd byte-identical (bypass transparency proven in production).
- 5 ledgers (Provenance + CC0/RI rows the gate requires); audit Phase 9
  loops extended (ledger/sha/render/floor).

## Gates

- 7/7 909 tests green. Full `ri_audit.sh` 0/0 — all 14 phases.
