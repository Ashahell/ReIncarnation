# §12.6b — 909 decouple: flam bit, OH-wins hats, linear mix

**Date:** 2026-09-25 (host lane; devices untouched)
**Scope:** review §4.3 items 3 + 5 + §2.4 (909 clip). Closes §12.6.
**Disposition:** New (TDD RED-first with per-family revert-checks)

## Production changes

- Flam bit + width decoupled from the accent value (`flam`/`flam_width`;
  `rb909_arm_flam` for the future FLAM-event consumer — NOTE→FLAM event
  order); accent==2 keeps arming the compat path (all flam/accent pins
  green unmodified).
- Hat rules (manual p. 35): OH trigger always steals CH; CH trigger cuts
  OH only past the 50 ms same-step window (age-gated); same-step either
  order renders OH-identical. `t1_909` §6 deliberately re-contracted.
- Shared hat level: one call writes both trims (panel single-knob staged
  §12.10).
- 909 clip removed (manual p. 23, same law as 808).

## Held with justification

- Velocity off/low/high: unwirable end-to-end without scheduler + RBNG step
  support (§12.7); accent 0/1 pins hold meanwhile.
- Analog-vs-ROM split: sample-layer architecture KEPT for all 11 (baked
  defaults = load-time render; D1 determinism, CPU budget, mod contract);
  analog character lives in the baked recipes (sweeps/clicks).

## TDD record

`t45_909_decouple` (link-RED → PASS): armed flam at width, compat guard,
OH-wins both orders + late-choke exactness, shared level jointness,
two-voice linearity with clip-region guard. Revert-checks on all three
behaviors (arm no-op, symmetric kill, tanh clip → FAILs → restore → PASS).
One test-ordering fix (arm after trigger = event flow; pre-trigger arm
clears by anti-reuse design).

## Golden fallout: NONE

- Full `ri_audit.sh` 0/0 with zero re-baselines (fixtures trigger single
  unaccented voices under the clip threshold; flam/hat paths untouched).
- ch.md/oh.md steal rows updated (audit's `steal` grep still matches).

## Gates

- 8/8 909 tests green (t1 + 5×t24 + t44 + t45). Full `ri_audit.sh` 0/0.
