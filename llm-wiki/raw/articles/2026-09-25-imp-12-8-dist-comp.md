# §12.8: dist 2x oversample + comp ratio/GR meter (m61)

**Date:** 2026-09-25 (host lane; devices untouched)
**Scope:** review §2 table (`fx.c:ri_fxdist_render` row) + §3.2 Dist/Comp rows.
**Disposition:** New (TDD RED-first, t50)

## Dist 2x oversample

Linear-mid evaluate + box decimate, prev-sample state streams across
block calls (no allocation; kernels only). Output law untouched (the
review's "fixed output law" critique is NOT this checkbox — DC norm
FS 1.0 → 1.0 still pinned ≤1e-6, so no level jump on engage).
Measured on 5 kHz sine @ full drive: 13 kHz fold 0.0276 vs naive
0.0677 (2.5x alias reduction).

## Comp ratio + GR meter

Ratio knob 0..127 → 1..20 linear (starting law, pending §8 ear-fit);
default stays 4:1 so existing behavior is unchanged. Make-up
re-derives on either knob set (new `ri_fxcomp_makeup` helper).
GR meter = block min compression gain in dB via `ri_log2`
(0 dB quiet, −9.5 dB hot @ thresh 64, floored −96 dB, read/reset).
Ratio 1:1 short-circuits to g=1 (kills a 1-ulp fp ghost the meter
caught: `thresh+(env-thresh)` ≠ env exactly).

## Gaps closed en route (both real, both tested)

- Dist had no knob-preserving reset for its new state: t1 §9
  restart-replay caught it ("chain nondet", first-sample only).
  New `ri_fxdist_reset` (prev=0, knobs survive), wired into
  `RiFXReset`, t1 restart protocol extended (bound untouched).
- Wrapper PCF pattern clamp was 53 (m59 left it): pattern 54
  unreachable via `RiFXSetParam`. Now 54. GUI knob range (§12.10)
  still pending separately.

## Golden fallout: fx-chain ONLY, root-caused

- `tests/golden/pcf/fx-chain.wav` re-baselined (fixture runs dist
  48/16 engaged): peak diff 3295 LSB, relative −38 dB (1.2% RMS).
  pcf-sweep + fx-delay bit-identical (dist bypassed there).

## Gates

- t50 RED (undeclared APIs) → GREEN incl. the ulp + reset fixes.
- t1/t25_dist/t27/t46/t47 green unmodified (except t1's restart line).
- Full `ri_audit.sh` 0/0.
