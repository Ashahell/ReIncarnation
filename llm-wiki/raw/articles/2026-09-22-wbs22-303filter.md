# 2026-09-22 — WBS TC-2.2.1 filter response (TDD property pin)

> Source: session evidence (test output, margin probe, mutant run, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. TDD property pin (PASS watched on frozen code, scratch-mutant
caught, GREEN verified, audit 0/0).

## What
- `tests/unit/t22_303filter.c`: reso knob min (asserts `reso_k == 0.0`
  endpoint), cutoff 1000 Hz direct, 20 log-spaced sines C0..C6 at 0.1
  peak through `rb303_filter_step`; steady-state RMS (last 0.5 s of
  2 s) vs an INDEPENDENT float64 reference implementing the documented
  Appendix B linear form (tan g, feedback HPF, DRIVE, 2x interp
  substeps; tanh skipped as identity — valid to 0.3% at 0.1). Assert
  within +-1 dB per the TC.
- Reference written from code comments/ledger, never by copying voice
  code: it catches coding deviations, not shared misunderstandings
  (stated openly). The dive's sketch equations (1-exp g, no HPF/drive/
  oversampling) were deliberately NOT used — the code documents why
  it differs (ledger D1).
- Wired with the 303 audit phase. No production code changed.

## Proof
- PASS first run; margin profile measured via scratch print (deleted
  after): worst -0.029 dB at 1047 Hz vs +-1.0 budget (34x margin),
  smooth monotonic drift with frequency (float32-vs-64 + tanh-linear
  residue, as expected). Not brittle.
- Load-bearing proven by scratch-copy mutant (g × 1.5 in a scratch
  copy — frozen file untouched): 5 upper freqs fail up to +4.4 dB,
  exit 1. Mutant discarded after.
- Full `ri_audit.sh` 0/0.

## Files (uncommitted)
- `tests/unit/t22_303filter.c`, `scripts/ri_audit.sh` (303 phase).

## Standing 2026-09-22 update: Dell run GREEN (TC-2.2.1 on reference hardware)
- Cross-built for ABIv11 (v11 gcc + SDK, `-ldos -lexec`, spec `-lm`;
  32640 B, task.resource=0) → `RAM:filter_dell`, session 750:
  `PASS t22_303filter`, rc=0 in 2263 ms, agent alive.
- All 20 frequencies within ±1 dB ON HARDWARE. Side proofs: AROS libm
  (sin/log10/pow/sqrt) is correct (the reference depends on it — a
  broken libm fails the comparison, not silently passes); ri_* kernels
  behave identically across the ABI boundary at 0.1 amplitude.