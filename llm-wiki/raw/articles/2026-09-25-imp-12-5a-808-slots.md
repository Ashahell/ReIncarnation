# §12.5a — 808 structural: 16 sounds, 11 slots, maracas, per-sound gain

**Date:** 2026-09-25 (host lane; devices untouched)
**Scope:** review §4.2 items 1–2 + §10.2 placeholder wiring (first vertical
third of §12.5; metal freqs/choke/BD/SD stay open).
**Disposition:** New (TDD RED-first with per-family revert-checks)

## API reshape

- Sounds 0–15 (MA added), 11 slots with last-wins selection (defaults
  BD SD LT MT HT RS CP CB CY OH CH); `render_mix` walks slots in order
  (the mix law); `triggered` mask u16→u32 (pins updated 0x7FFF→0xFFFF).
- Per-sound `level` trim (knob/127, default 1.0) and `accent_amt` (default
  0.5 → legacy ×1.5 bit-exact); LEVEL/ACCENT knobs wired (DECAY/TUNE were
  already); SNAPPY/TONE stay placeholders for §12.5b.
- Maracas: HP-6kHz noise, 40 ms envelope + 1 ms click, excite-scaled.
- Kit-global accent knob staged for the panel inventory (§12.10);
  per-voice amount keeps twin-voice independence meanwhile.

## TDD record

`t41_808_slots`: (a) collision last-wins (96000-FAIL revert-check),
(b) MA audible + accent + body-vs-click (the body assertion caught a
stale-object false-green mid-course), (c) level law exact ratio,
(d) accent law 2.0/1.5 exact ratio, (e) 11-slot storm == selected solos.
t34 rewritten slot-aware; mask pins in t1_808/t23_808storm deliberately
updated; NVOICES removed (loud compile break, all users fixed).

## Deliberate re-baselines (each root-caused)

- 15 single-voice goldens: byte-identical (one voice + exact-zero slots).
- storm: slot-order sum of 11 voices (RMS 0.30→0.24, peak 1.0 both).
- ma.wav: new (no predecessor), peak 0.17.
- Audit Phase 8 loops + sidecars extended to the 16th sound; ma.md ledger
  (EXCITE row the gate requires).

## Gates

- 11/11 affected tests green. Full `ri_audit.sh` 0/0 — all 14 phases.
