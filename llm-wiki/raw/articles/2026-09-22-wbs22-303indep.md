# 2026-09-22 — WBS TC-2.2.6 two-instance independence (TDD property pin)

> Source: session evidence (test output, mutant runs, audit), compiled by agent
> Collected: 2026-09-22
> Published: 2026-09-22

## Disposition
New. TDD property pin (source audit + RED behavior, mutant-caught,
GREEN verified, audit 0/0).

## What
- Source audit first: no file-scope mutable state in `rb303.c`,
  `kernels.c`, `params.c` — all state lives in `RB303Voice`, so
  instances cannot share state by construction.
- `tests/unit/t22_303indep.c`: fresh voice A renders notes X twice
  with an active different voice B in between (separate buffer,
  discarded); A1 must equal A2 bit-exactly (crosstalk -inf, stronger
  than the TC's -90 dB). Energy guards on both (a silent voice would
  pass vacuously — B-with-slide tripped it once: slide-from-silence
  renders nothing, so B plays plain).
- Wired next to the 303 tests in the audit (not Phase 6b — that's
  seq-scoped).

## Proof
- PASS with energy + equality green.
- Load-bearing proven by clobber mutant (B renders into A1's buffer
  → B-silent FAIL + crosstalk FAIL max diff 0.81 from sample 0, exit
  1) with revert verified. (A first mutant attempt had a typo'd macro
  and failed compile — correctly discarded as non-evidence.)
- Full `ri_audit.sh` 0/0 (after two transient quota-race FAILs on the
  AROS-build phase — standalone green both times, third audit run
  clean; the other session is bursting heavily today).

## Files (uncommitted)
- `tests/unit/t22_303indep.c`, `scripts/ri_audit.sh` (303 phase).