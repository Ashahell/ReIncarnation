# 2026-09-23 — M2.5 mod pack: TC-2.12.1/TC-2.12.5 pins (no production change)

> Source: session evidence (test output ×2, audit 0/0 ×1 final + 1 pre-change baseline self-invalidated, see Proof), compiled by agent
> Collected: 2026-09-23
> Published: 2026-09-23

## Disposition
New. Gap analysis of TC-2.12.x against landed Task 13 code found
two host-pinnable cores without dedicated pins: the shipped
classic-01 pack as a whole (TC-2.12.1 — t1 covers reserialize on
the file, but never inventories all 14 layers nor loads every
layer's samples) and S909-driven sound change (TC-2.12.5 —
t1_909 §7 proves render-diff on synthetic layers, never on
shipped binary data). One pin (`t30_modpack`, wired in the
formats audit phase after `t1_formats`) + ledger lock. Green
first run (property pin) — no production code written. Out of
host scope with reasons: TC-2.12.2 already measured 500/500
(`fuzz.md`, nothing to add), TC-2.12.3/2.12.4 device-side
(art-fallback rule + CPRG hook already pinned host-side).

## What
- Inventory: 14/14 layers sane (44.1/48 kHz rates only, frames >
  0, lo ≤ hi) via `rbnm_pack_layers`.
- Every layer's samples load finite via `rbnm_load_smpl` (no
  empty/shorted layer hides in the pack).
- Round-trip: reserialize byte-identical (the TC-2.12.1 "saves
  unchanged" half on the shipped file).
- S909 render-diff: shipped BD-LOW vs BD-HI through
  `ri_layer_mix` differ 4095/4096 samples with bit-identical
  remix (the pack changes sound per S909 chunk, TC-2.12.5).
- `docs/evidence/formats/rbnm-full.md` → LOCKED at
  TC-2.12.1/TC-2.12.5 (first Status line the file ever carried).

## Proof
- TDD: pin written first, green on frozen code (property pin,
  t22 precedent). Layer IDs (BD-LOW/BD-HI) confirmed by the
  passing load itself, not assumed.
- Full `ri_audit.sh` 0/0 final over the committed tree (0 FAIL
  lines).
- Process repeat: the pre-change baseline self-invalidated AGAIN
  (rc=1, `line 417: -lm: command not found` — the run reached
  the bench region after the t30 audit insertion shifted lines
  under it; current line 417 is a valid bench-invocation line).
  Second occurrence of the m10 lesson, now strengthened: launch
  the baseline and make NO edits until it completes — or accept
  the baseline as void up front. The failure proves nothing
  about any tree; the final green stands alone.

## Files
- tests: `tests/unit/t30_modpack.c` (new; no `engine/` file touched)
- env: `scripts/ri_audit.sh` (formats phase t30 line)
- ledgers: `docs/evidence/formats/rbnm-full.md` (Status + section)
- wiki: this article + `llm-wiki/log.md` + `llm-wiki/index.md`

No external websites consulted: contracts are WBS TC-2.12.x +
the shipped pack bytes themselves — all local; nothing was open
that needed the web. spirv-val vacuous (no SPIR-V in this repo —
noted honestly per standing).
