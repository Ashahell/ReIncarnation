# docs/evidence — parity ledger

Per-device subdirectories (`303/`, `808/`, `909/`, `pcf/`, `sequencer/`,
`gui/`, `formats/`) hold the parity-matrix ledger rows for each
compatibility claim (evidence classes E0–E5, per the implementation plan).
