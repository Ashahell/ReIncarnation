# Soak log — Task 14 (gate G14), P-19

**Host:** frostmourne, Linux 7.2.3-arch1-3 x86_64 GNU/Linux, 8 CPUs,
GCC 16.2.1, flags `-std=c99 -O2 -Wall -Wextra -Werror -pedantic
-ffp-contract=off -fno-unsafe-math-optimizations -ftrapv`.
**Harness:** `scripts/ri_soak.sh` (bench + render CLI over storm /
mix-four / fx-chain / pcf-sweep / 909pack-bd / corpus s01–s10 /
tempo 140-174-90 + MODR prompt every iteration; per-render md5 vs
first-pass pin; any nonzero exit or md5 drift = 1 underrun event).
**Scope honesty:** offline soak = determinism + throughput under
switching load. Device underruns need the M1.1 box (beta-exit.md);
"overnight" is NOT claimed — the runs below are the runs that ran.

## Run 1 (evidence): 300 s deadline, COMPLETE, exit 0

- Date: 2026-09-21T00:00:13Z. Out: /tmp/ri/soak-evidence (SOAK.log).
- bench 45000 blocks (60 s audio): cpu 12.152 s, ratio 0.2025,
  headroom 4.9x, checksum ffc9296c5325d1fb DETERMINISTIC (same
  checksum as every other run this session — cross-run stable).
- Loop: **iterations=200, underruns=0**, slowest render storm 301 ms.
- Result: SOAK PASS.

## Run 2 (incident): 600 s deadline, full duration covered, no summary

- Date: 2026-09-20T23:48:03Z. Out: /tmp/ri/soak-long (SOAK.log).
- The executor edited `scripts/ri_soak.sh` (bench-block override)
  WHILE this run was in its loop; bash re-parses the script file, so
  the closing summary line died with a syntax error AFTER the loop
  had covered the full 600 s deadline (log last write 23:58:30Z).
- The log holds **0 UNDERRUN lines** across the full duration;
  iteration count unrecoverable (summary lost). Recorded as process
  evidence, NOT as the gate run: the gate run is Run 1 above, which
  executed the identical final script unmodified start to finish.
- Lesson (probe-evidence discipline): never touch a script a
  background run is executing; copy first.

## Underrun definition (both runs)

Failed-or-nondeterministic renders. Count: 0 and 0. The null-backend
xrun counter (AUQA_XRUN_COUNT) is not exercised here — file export
and the null drain share `au_render_frames` (one renderer, Task 6),
so determinism of the export path IS determinism of the live path's
engine half; the device half stays M1.1-deferred.
