# Prompt for opencode — §12.10 G8: Mods/skins, zoom finish, GUI acceptance (+ carry-overs)

> Paste everything below the line into opencode as one task. It is self-contained: every path, rule, command and open item it needs is in here. Read it end to end before you touch anything.

---

## 0. Who you are and what you are finishing

You are continuing the **ReIncarnation** project (`/home/miller/Work/projects/ReIncarnation`, branch `main`): an AROS (x86-64, ABIv1) re-creation of **Propellerhead ReBirth RB-338 2.0.1** — TB-303 ×2, TR-808, TR-909, mixers, FX, pattern/song sequencer — faithful to ReBirth first and to the original Roland hardware second. You own §12.7/§12.9 (engine, pattern model, song track, player). Another session (Claude) built the GUI slices G1–G7 of plan `docs/superpowers/plans/2026-09-25-gui-parity-plan.md` (§12.10). **Your job now is G8, the last GUI phase, plus four carry-overs listed in §6.** When G8 closes, §12.10 closes.

Fidelity order, always: **E1 = ReBirth RB-338 2.0.1 Owner's Manual** (printed page numbers; a text copy can be made with `pdftotext` from the manual PDF the repo cites) → original hardware → owner decision. Where the manual is silent, you make an **E0** decision, write it down in the relevant ledger under "Decisions (E0 — owner may overrule)", and move on. Never invent an E1 citation; quote only what you grepped.

## 1. Read first (in this order)

1. `llm-wiki/index.md` (§ "GUI (§12.10)") and the records it links for G1–G7, G5 key precedence, G6a live state, G7 remote MIDI.
2. `docs/superpowers/plans/2026-09-25-gui-parity-plan.md` — §G8 is one line: *"Mod art through the MCC classes, zoom crispness, the ReBirth-101 tutorial checklist in `docs/evidence/gui/acceptance.md`."* This prompt expands it.
3. `docs/superpowers/specs/2026-09-20-reincarnation-spec.md` §13 (GUI, widget reuse, RBNM/RBNG, mods), §17 (degraded modes, row 3 "missing mod"), §1 (clean-room legal note), OPEN table (OPEN-10).
4. `docs/evidence/gui/`: `acceptance.md`, `panel-geometry.md`, `section-canvas-proof.md`, `keyboard.md`, `live-state.md`, `midi.md`.
5. Manual chapter 11 "Using Mods" (p. 88–97) and chapter 12 "Mod dialog / Adding Mods / Creating Your own Mods" (p. 94–97+).
6. The code map in §2 below.

## 2. What exists (do not rebuild it)

GUI commits on `main` (all audited 0/0 at commit time): `f130142` G1 registry · `8ae2c05`/`32c9595`/`89a1dab` 303 · `939c01f` 808 · `2892026`/`ea22ce3` 909 · `71c2f6c` mixers+master · `b30e576` FX · `31ed17b` transport+pattern · `64a8a30`/`4849786` G5 keyboard+focus · `9c65a84` G6a live state · `d576987` G7 remote MIDI.

- `gui/ctlreg.{h,c}` — 243 controls, 18 sections, reg_id = section<<8|index, kinds KNOB/FADER/SWITCH/BUTTON/LED/STEP/SELECTOR/DISPLAY/METER, all 99 Appendix C CCs, engine bindings (unbound controls render disabled). **Single source of truth for every control.**
- `gui/panelgeo.{h,c}` — geometry in **Q units** (quarter pixel of the manual figure); base scale 2 window px per figure px; zoom indices 0=1x, 1=1.5x, 2=2x, 3=`RI_GEO_ZOOM_COMPACT` 0.75x (the Transport is 842 px at 1x); item shapes KNOB/RECT/LED/LEGEND/DIVIDER/OPTION/STEPPER; `ri_geo_hit_opt`, `ri_geo_focus_bar`.
- Behaviour (pure C, host-tested): `sect303/808/909/sectmix/sectfx/sectpat/secttr`, dispatch `sectui` (`ri_sui_init/press/set/reset/step/value/led/display`, `ri_sui_bind_board`), `keymap` (Appendix E), `panelui` (one front panel: focus, keys, taps, live feed, mixer/FX pointers for MIDI), `livestate` (sample-count position law, meter/GR scales), `midimap` (Standard Mapping).
- Canvas: `gui/widgets/rsection.mcc.c` — ONE Zune `MUIC_Area` subclass renders any section from registry + geometry + sectui. **All section art is currently procedural drawing in this file** (per-section `bg_*` functions + per-kind drawing: knobs, faders, LED digits, keys, levers, meters). Colours are an enum of RGB tokens obtained with `ObtainBestPen` in `MUIM_Setup`.
- Older widget classes still present: `rknb/rstp/rlbl/rfdr/rlvl.mcc.c` (knob with real procedural art in `gui/knob_art.{h,c}` + `knob_blit`), used by the older proof apps (`app/panel909.c`, `stepproof.c`, `knobproof.c`). `app/main.c` is still a bare Intuition window.
- Proof app: `app/sectproof.c` → `RISECT [303|808|909|mix|fx|tr|keys|live|remote] [demo]`; `app/midisend.c` → `MIDISEND <cluster> <script>|SELFTEST`. Built by `bash scripts/ri_build_aros.sh sections` → `/tmp/ri/aros/RISECT`, `/tmp/ri/aros/MIDISEND` (gates: 0 unresolved symbols, 0 `mov %rax,%r12`).
- Host tests `tests/unit/t60_ctlreg … t73_midimap` — all pass, each mutation-checked, **but NOT yet wired into `scripts/ri_audit.sh`** (carry-over C1).
- Formats: `project/rbng.{h,c}` (song; `MODR` chunk = mod reference: name + SHA-256 hex + vers, up to 16), `project/rbnm.c` (RBNM, the mod/sample-set container used by the 909 path). Spec paths: mods live in `SYS:Classes/ReIncarnation/Mods/`.

## 3. Hard rules (non-negotiable)

1. **TDD with a behavioural RED.** Scaffolding RED (missing header) does not count: add stub bodies, run, record the first failing `RI_ASSERT` line in the commit body, then GREEN. Every new pure module gets a `tests/unit/tNN_*.c` (next free number after the highest existing test — check, you already use t74) and **at least one mutation proof** per invariant (mutate, see FAIL, revert, see PASS; record the failing line).
2. **Full `bash scripts/ri_audit.sh` = `AUDIT 0/0 PASS` before every commit.** If another session's WIP breaks the live tree, verify your slice in a scratch `git worktree` at HEAD + your files only; the worktree needs a sibling `Vulkan4Aros` symlink because `ri_build_aros.sh` finds the toolchain via `../Vulkan4Aros`. Never edit `ri_audit.sh` while an audit is running.
3. **Commit only your own files**; never `git add -A`. Commit messages end with `[§12.10 G8]` (or the carry-over tag) and the line `Co-Authored-By: OpenCode <noreply@opencode.ai>`. `git pull --rebase --autostash` before `git push`.
4. **Clean-room (spec §1, §13, LOCKED):** never copy ReBirth or Roland pixels or samples. Measure proportions, style-match, author original art. No ReBirth `.rbm` file content, no ROM dumps, no third-party samples ship. Importing user-owned ReBirth files is **OPEN-10 — legal review first**; do not write an `.rbm`/`.rbs` importer in this phase (you may record its design as a note).
5. **Coexistence:** GUI work touches `gui/`, `app/`, `tests/unit/`, `docs/evidence/gui/`, the `MOD_gui` line of `scripts/ri_build_host.sh`, the `sections` block of `scripts/ri_build_aros.sh`, and `scripts/ri_audit.sh` (you own it). `scripts/` must keep exactly 5 files (audit gate). No libm in engine code; no allocation/locks in render paths (audit greps literally — avoid the string `free(` in engine comments).
6. **AROS/Zune lessons (all learned the hard way):** obtain pens per screen in `MUIM_Setup` (`ObtainBestPen`; RGB pens are ignored on palette screens) and release in `MUIM_Cleanup`; add event handlers in `MUIM_Setup` on `_win(obj)`, never `_window`; paint unconditionally on every `MUIM_Draw`; `GetAttr` stores a full `IPTR` (a `LONG` target smashed the stack); fonts do not scale with zoom (anchor legends to controls, see `text_at`); canonical loop = `NewInput` + `Wait`; exactly one raw-key owner canvas per window.
7. **Fidelity citations:** every behaviour you add cites a manual page; every place the manual is silent gets an E0 row in the ledger.

## 4. Lanes and how to prove things

- **riqemu1** (private AROS lane, ABIv1, installed on DH0): launcher `/home/miller/Work/vms/start_riqemu1.sh`; agent spool `/tmp/spike_spool_priv`, spike port 9295, HMP monitor `tcp 127.0.0.1:4477` (no `nc` on the host — use a Python socket). Screen **1280x1024**. **Owner rule: every reboot/reset of riqemu1 must come back at 1280x1024** — GRUB default is pinned to "AROS64 with VESA Gfx @ 1280x1024-32bpp" (`DH0:boot/grub/grub.cfg`, backup `.orig`); verify with a monitor `screendump` after any reset. Before a reset, move pending `jobs/*.json` out of the spool (otherwise the agent replays a poison job) and wait ~15 s for SFS to flush. Submit jobs with `python3 /home/miller/Work/projects/Vulkan4Aros/scripts/spike_server.py submit --spool /tmp/spike_spool_priv --put LOCAL:REMOTE --exec "…" --get REMOTE:LOCAL --wait N`.
- **Input on the lane:** mouse injection (agent ui-click, monitor mouse) does **not** reach windows. **Keyboard does:** monitor `sendkey <qcode>` delivers real PS/2 keys to the active window (G5/G6a/G7 proofs). MIDI can be injected through CAMD with `MIDISEND`. Use these for every interactive proof; a true mouse test needs a human.
- **Evidence capture:** monitor `screendump FILE.ppm` (full 1280x1024) or agent `--ui-capture FILE,2` (scale 1 is refused at 1280x1024). Read logs with `--get`, never `Type` (exec output is truncated at ~80 lines). A file your program holds open with `MODE_NEWFILE` is locked — close it first.
- **Dell E6320** (real HDA, the iteration reference box): currently unavailable (USB stick backup). When it returns it is the lane for the human-hand acceptance pass and real audio.
- **riqemu1 lane state you inherit:** `DH0:Libs/camd.library` is the fixed build (original `camd.library.orig`); `DEVS:Midi/debugdriver` is parked at `SYS:debugdriver.parked` (see C3).

## 5. G8 scope — tasks, in order

### G8.1 Skin architecture ("Mods", appearance half) — design first, then code
E1 (p. 88–97): a Mod changes the look of the panel and the drum sounds; it **never** changes function, control placement, the synth sound or the FX. The Mod selection is saved in the song (RBNG `MODR`); a missing mod on load → spec §17 row 3 ("Warn … locate it?", else load the default mod, mark the song dirty, never substitute silently). Mod switch is idle-only (spec §13 909 note). Spec §13: skins load through `picture.class` datatypes and are applied **only via MCC subclassing** (no new BOOPSI gadget classes); day-one mods **"Classic" (default) and the LOCKED second skin "808-RI"**, plus a blank template + AmigaGuide SDK; mods live in `SYS:Classes/ReIncarnation/Mods/`.

1. Write `docs/superpowers/specs/2026-09-26-skins-design.md` (short, owner-reviewable) answering: the skin model on top of `RSection` (geometry stays in `panelgeo` — a skin may only restyle, never move); what a skin supplies per section (background image, per-kind art: knob frame strip like ReBirth's "one frame per knob position", fader cap, key/step up/down/lit frames, LED on/off, digits, lever states, legend font/colours); fallback rule (any missing part → the procedural Classic drawing that exists today, per part, so a partial skin is legal); on-disk form (a directory per mod with an IFF/ILBM or PNG-via-datatypes image set + a small text/IFF manifest, SHA-256 identity for `MODR`) and the RBNM relation for the drum-sound half (sound half stays in the engine owner's scope — you may only reference it); zoom policy (G8.3); memory (load on select, release on switch); the missing-mod path. List every E0 choice. **Stop and put the design in the tracker as "awaiting owner review" if a choice is not reversible** (file format, directory layout); proceed with reversible parts.
2. Pure, host-tested core `gui/skin.{h,c}` (tNN): manifest parser (bounds-checked, fail-closed, unknown keys ignored), part lookup with per-part fallback to Classic, SHA-256 identity via the existing `project/sha256.c` (already in `MOD_formats`; if linking it into `MOD_gui` crosses the coexistence line, compute identity in the loader and pass it in), `MODR` round-trip helpers (read the current mod ref from a song, write the selected one) without changing `rbng_write_song/read_song` signatures (audit `check_sig` pins them).
3. AROS loader (AROS-only TU in `gui/widgets/` or `app/`): `datatypes.library` → `picture.class` → BitMap per part; freed on switch; failures degrade to Classic per part with a readout note. RSection draws a skin part when present (blit with mask/alpha as the datatype allows), else its current procedural path. Keep pen handling correct for both paths.
4. "Classic" = today's procedural look (no files needed). "808-RI" = **original** art authored by you as data (e.g. generated procedurally into the skin format by a host tool under `tools/`, like `knob_art` does) — never traced from ReBirth. Blank template = manifest with every key documented and no images.
5. Mod selection UI: a minimal Mods menu or a proof-app switch (`RISECT … mod=<name>`) plus the Ctrl+M shortcut already decoded by `gui/keymap` (`RI_KM_SELECT_MOD`, currently decoded only). Missing-mod requester per §17 row 3.
6. Proof on riqemu1: the same section rendered under Classic and 808-RI (screendumps side by side), a partial skin showing per-part fallback, a missing-mod load showing the warning path; every control still works identically under both (use `RISECT keys`/`remote` flows — the readouts must match between skins).

### G8.2 Zoom finish
Acceptance rows TC-2.11.x: "1x / 1.5x / 2x all crisp (downscale from 2x masters)" and "Knob travel stays 150 px of *screen* travel at every zoom". Today every zoom is procedural (crisp by construction) and the compact 0.75x exists for 800-px screens.
1. Skin art: author masters at 2x; 1x/0.75x/1.5x are produced by a **filtered downscale done once at load/zoom-change time** (host-testable pure resampler, e.g. box/bilinear on RGBA with premultiplied alpha — no libm needed; tNN pins exact pixels on a small fixture), never per frame.
2. Knob/fader drag law at zoom: check `rsection.mcc.c` uses `ri_knob_drag_to_value` in screen pixels — the 150 px full-travel rule must hold at every zoom (host test on the pure law; device check with keyboard-driven values is not possible for drags — mark the drag row for the human pass).
3. A zoom switch in the proof app (`zoom=0..3`) and captures at each zoom for one section per family (303, 808, mixer, FX, transport) under Classic, and one under 808-RI.

### G8.3 Acceptance pass (`docs/evidence/gui/acceptance.md`)
1. Update the document to the current GUI: it still describes the Task-12 knob widgets; add rows for the section canvases (G2–G7) and link the evidence ledgers. Tick every row you can prove, with a pointer to the evidence (capture/trace/test). Leave rows that need a human hand or real audio unchecked with the reason.
2. The **ReBirth-101 tutorial workflow (≥4/5 to pass)**: (1) 303 cutoff sweep and hear it, (2) program a 16-step 303 pattern (pitch mode + slide), (3) program a 909 accent + flam step, (4) ride a bus fader + mute/solo the 808, (5) switch patterns under transport without stopping. On riqemu1 you can drive (2), (3), (5) and the GUI half of (4) through `sendkey` (Appendix E keys) and `MIDISEND` (Appendix C notes/CCs); hearing needs audio (G6b/C2 + the Dell). Record exactly which parts are machine-proven and which await the human pass. **Note: ReBirth has no Solo** — the row says "mute/solo"; record that the mixer mute (p. 56) is the ReBirth control and the row's "solo" is out of E1.
3. The "Dummy panel (TC-2.9.5)" generality row: prove a two-control dummy section can be added through the registry + geometry + sectui path with zero framework edits (host test + one capture), or record precisely what framework edit it needs (this feeds the owner's extensible-device-rack goal, review §5.6 / decisions D-k/D-l).
4. Human sign-off block stays for the owner.

### G8.4 Close §12.10
Tracker `docs/2026-09-24-improvement-todo.md` §12.10: tick what is done, list what remains (G6b, human pass). Wiki ingest (the project's karpathy-llm-wiki conventions: raw record in `llm-wiki/raw/articles/`, index entry under "GUI (§12.10)", log entry at the top). Commit + push.

## 6. Carry-overs you own (do these too; each is its own commit)

- **C1 — audit wiring (plan G1.6, overdue):** add `t60_ctlreg … t73_midimap` (and your new tNN) to `scripts/ri_audit.sh` in a GUI phase block (one `bash "$ROOT/scripts/ri_build_host.sh" test <name> >/dev/null || { echo "FAIL: <name>"; exit 1; }` line each, mirroring Phase 7b), add `gui/ctlreg.c` as the only file allowed to define control legends (grep gate), and add the AROS-only GUI TUs (`gui/widgets/rsection.mcc.c`, `app/sectproof.c`, `app/midisend.c`) to the audit's AROS compile-only loop or to a `ri_build_aros.sh sections` invocation inside the audit. Prove each new gate is load-bearing with one mutant.
- **C2 — G6b live binding (you own the engine):** feed `ri_panel_live` from the render task's sample position (`RiSeqMasterClock` / the AHI PlayerFunc frame count) instead of the Intuition `CurrentTime()` stand-in in `app/sectproof.c live`; add per-section and per-FX-unit peak taps in the engine (the mixer already has the P-16 `ri_meter_*` ballistics) and feed `ri_smix_meter_set`/`ri_sfx_meter_set` through `ri_live_meter_level`; route `ri_engine_comp_gr` through `ri_live_gr_level`. Taps must then record against the audio position (the current ≤100 ms IntuiTicks staleness goes away). Needs a lane with audio: the Dell's HDA, or add a sound device to riqemu1 (QEMU AC97 or ICH HDA + `-audiodev`, plus the matching AROS AHI driver) — if you change `start_riqemu1.sh`, keep the 1280x1024 rule and note it in the lane record.
- **C3 — camd.library (AROS side):** the fix for the x86-64 varargs bug (`workbench/libs/camd/strings.c` `mysprintf`: `&fmt+1` → `va_list` + `VNewRawDoFmt(fmt, RAWFMTFUNC_STRING, …)`) is applied only as an uncommitted edit in `Vulkan4Aros/src/abi/v1/AROS` and saved as `docs/evidence/gui/2026-09-26-camd-mysprintf-x86_64.diff`. Put it into the Vulkan4AROS v1 patch series (`src/abi-patches/v1/aros/00NN-camd-mysprintf-varargs.diff`, which currently lives on branch `t8-workgroup-size` — coordinate with that branch's owner; never commit to someone else's branch without asking the owner) and prepare an upstream AROS PR via `tools/aros-upstream/prs.py` (one change per PR, x86_64-pc tested). Then investigate the exposed hang: with correct paths, camd init really loads `DEVS:Midi/debugdriver` and the first `OpenLibrary("camd.library")` blocks — find why (`init.c` / `drivers.c`, the driver's init, `CamdWait`), fix on the AROS side, restore `SYS:debugdriver.parked` to `DEVS:Midi/debugdriver` on riqemu1, and prove `MIDISEND SELFTEST` + `RISECT remote` still pass.
- **C4 — Stop law vs E1 (your §12.9 transport):** recorded in `docs/evidence/gui/panel-geometry.md` (Transport section): (1) with the song stopped, the engine's first Stop click only arms the sequence, while p. 145 moves the position to the Loop Start; (2) the manual's exception (position before the Left Locator → song start) is not modelled. Decide with E1, fix `engine/seq/transport.c` under TDD if you agree, and update the GUI tests (`t69_secttr` expects the current engine law — adjust it with the engine, in the same commit).

## 7. Owner decisions still open (do not decide these yourself; list them in your final report)

- Dist exclusivity: p. 59 "four distortion units… all sections can use the distortion" vs p. 157 "One section at a time" (GUI + engine currently radio).
- G5/G7 E0 items: Ctrl + Right-Amiga menu modifier, focus arrows stop at the ends, both keyboard options default off, focus-bar position, CC value laws, LED timings, the p. 199 Stop/Record typo resolution (keys column: 70 = Stop, 71 = Record).
- 909 tap level (low), comp GR full scale (20 dB), meter floor (−36 dBFS).
- OPEN-10 (ReBirth file import) — legal review.

## 8. Definition of done for this task

- G8.1–G8.4 and C1–C4 each landed as its own reviewed commit with tests, mutation proofs, evidence and audit 0/0 — or, where blocked (owner review, Dell, audio lane, other branch), stopped at a clean point with the blocker written into the tracker and the report.
- `docs/evidence/gui/acceptance.md` reflects reality: every ticked row points to evidence; every unticked row says why.
- Wiki ingested (both wikis where AROS-level findings arise: ReIncarnation `llm-wiki/`, and Vulkan4AROS `llm-wiki/` on `main` via a separate worktree if that repo is checked out on another branch).
- Final report to the owner: brief, results first — what landed (commits), what was proven how, what is blocked and on what, the open owner decisions (§7). No reasoning dumps.
